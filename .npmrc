# Supply-chain hardening: only install versions at least 7 days old.
# Honored by npm >= 11.10.0 (current npm is 10.9.3, so it is a no-op until upgraded).
# Escape hatch for urgent security fixes: set to 0 (do NOT leave it at 0).
min-release-age=7
