# Visuelle Playwright-Baselines sind grosse, haeufig wechselnde Binaerdateien.
# Ueber Git LFS gespeichert, damit die Repo-History nicht aufblaeht.
tests/visual/**/*.png filter=lfs diff=lfs merge=lfs -text
